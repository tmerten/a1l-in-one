The project allows to select date ranges. But it does not allow to select sprints (although sprints are present in the data model) as start and end date. I would like to add a separator in the dropdown and afterwards show the 5 last sprints as selectable entries.